.box-contents
  - @products.order('created_at DESC').limit(4).each do |product|
    .items-box
      = link_to product_path(product), method: :get do
        -# product_path(params[:id]), method: :get はtop表示されない. missing required keys: [:id]
        .items-box_photo
          -#= image_tag product.photos[0].variant(resize:'213x210').processed
          = image_tag url_for(product.photos[0]), class:"items-box_photo__image" 
        .items-box_body
          %h3.items-box-name
            = product.product_name
          .items-box-num
            .items-box-price
              ¥ 
              = number_with_delimiter(product.price)
            .items-box-icon
              %i.far.fa-heart
              8
          .items-box-tax
            (税込)
.view-all
  %a{ href:'', class:"text-center__inner"}
    すべての商品を見る

-# ⬆︎はtop表示されて、 
           missing required keys: [:id]  // ということは今利用しているlinkもidが存在せず、補うために`id:products`が代入されていると考えると筋が通る