.box-contents
  - @products.order('created_at DESC').where(category_id:(1..218)).limit(4).each do |product|
    .items-box
      = link_to product_path(product), method: :get do
        .items-box_photo
          = image_tag product.photos[0].variant(resize:'213x210',auto_orient: true).processed
        .items-box_body
          %h3.items-box-name
            = product.product_name
          .items-box-num
            .items-box-price
              ¥ 
              = number_with_delimiter(product.price)
            .items-box-icon
              %i.far.fa-heart
              8
          .items-box-tax
            (税込)
      -if product.purchaser_id.present? 
        .items-box_photo__sold
          .items-box_photo__sold__inner
            SOLD
.view-all
  %a{ href:'', class:"text-center__inner"}
    すべての商品を見る